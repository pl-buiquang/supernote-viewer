import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'net.upadvipa.supernoteviewer',
  appName: 'supernote-viewer',
  webDir: 'dist'
};

export default config;
